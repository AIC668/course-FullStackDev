# Dockerfile.dev

# 使用官方 Node 镜像
FROM node:13

# 设置工作目录
WORKDIR /app

# 将 package*.json 拷贝进容器（如果有）
COPY package*.json ./

# 安装依赖（如果没有 package.json 会报错，可以后续再补）
RUN npm install

# 安装开发依赖 nodemon
RUN npm install -g nodemon

# 拷贝当前目录的所有内容到容器中
COPY . .

# 默认运行 nodemon 启动 app.js
CMD ["nodemon", "app.js"]
